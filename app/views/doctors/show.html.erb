<!--page that displays the doctor's name, department, and appointments,
 with each appointment's date, time, and patient name (linking to the patient's show page)-->

<h1>Doctor <%= @doctor.name %></h1>
<p>Department: <%= @doctor.department %></p><br>
<h3> Appointments: </h3>

    <% @doctor.appointments.each do |appt| %>  <br>
    <li> <%= link_to appt.patient.name, patient_path(appt.patient) %> - on
     <%= appt.appointment_datetime.strftime('%B %d, %Y at %H:%M') %></li>


<%end%>
